import React from "react";

const Header = (props) => {
    return (
<div className="header">Employee Directory
 <p>Click on a column heading to sort or use the search box to filter/sort your results by first name.</p>
</div>
);
}

export default Header;
