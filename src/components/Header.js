import React from "react";

const Header = (props) => {
    return (
<div className="header">Employee Directory
 <p>Click on the location heading to sort by location, or use the search box to filter your results by name.</p>
</div>
);
}

export default Header;
